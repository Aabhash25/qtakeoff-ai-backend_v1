[Unit]
Description=Celery Service
After=network.target

[Service]
Type=simple
User=root
Group=root
WorkingDirectory=/var/www/quantity_takeoff_v4/backend
Environment="TESSDATA_PREFIX=/usr/local/share/tessdata/"
Environment="PATH=/var/www/quantity_takeoff_v4/backend/venv/bin:/usr/local/bin:/usr/bin:/bin"
Environment="TMPDIR=/var/tmp"

ExecStart=/var/www/quantity_takeoff_v4/backend/venv/bin/celery -A config worker --loglevel=info --concurrency=2 --pool=prefork

Restart=always
RestartSec=10
MemoryMax=2G
MemorySwapMax=2G

[Install]
WantedBy=multi-user.target

root@ssnai-backend:/etc/systemd/system# cat django-backend.service
[Unit]
Description=Django App via Gunicorn
After=network.target

[Service]
User=root
Group=root
WorkingDirectory=/var/www/quantity_takeoff_v4/backend
ExecStart=/var/www/quantity_takeoff_v4/backend/venv/bin/gunicorn config.wsgi:application --workers=2 --threads=2

Restart=always

[Install]
WantedBy=multi-user.target